import React from 'react';
import styled from 'styled-components';

import Tile from './components/Tile';


const Maps = ({config}) => {
    return (
        <div>hinsts here</div>
    );
};

export default Maps;